<h3 class="m-5">マイページ</h3>
<div class="mx-5">
  <span class="m-3">登録情報</span>
  <%= link_to "編集する", edit_public_customer_path(@customer.id), class: 'btn btn-primary' %>
  <div class="col-5 ml-3">
    <table class="table table-bordered m-3">
      <tho
      <tbody>
        <tr>
          <td>氏名</td>
          <td><%= @customer.last_name %> <%= @customer.first_name %></td>
        </tr>
        <tr>
          <td>カナ</td>
          <td><%= @customer.last_name_kana %> <%= @customer.first_name_kana %></td>
        </tr>
        <tr>
          <td>郵便番号</td>
          <td><%= @customer.postal_code %></td>
        </tr>
        <tr>
          <td>住所</td>
          <td><%= @customer.address %></td>
        </tr>
        <tr>
          <td>電話番号</td>
          <td><%= @customer.telephone_number %></td>
        </tr>
        <tr>
          <td>メールアドレス</td>
          <td><%= @customer.email %></td>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="my-3">
    <span class="m-3">配送先</span>
    <%= link_to "一覧を見る", public_addresses_path(@customer.id), class: 'btn btn-primary' %>
  </div>
  <div class="my-3">
    <span class="m-3">注文履歴</span>
    <%= link_to "一覧を見る", public_orders_path(current_customer.id), class: 'btn btn-primary' %>
  </div>
</div>
